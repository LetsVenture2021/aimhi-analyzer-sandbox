name: Behavioral Segmentation

on:
  workflow_dispatch:

jobs:
  segmentation:
    runs-on: ubuntu-latest

    steps:
      - name: Run segmentation agent
        run: |
          ./agents/user-experience-optimizer.sh "Segment users by behavior and propose personalized experiences."
