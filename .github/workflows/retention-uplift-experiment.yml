name: Retention Uplift Experiment

on:
  workflow_dispatch:

jobs:
  retention:
    runs-on: ubuntu-latest

    steps:
      - name: Run retention uplift agent
        run: |
          ./agents/user-experience-optimizer.sh "Design retention uplift experiments and propose A/B tests."
